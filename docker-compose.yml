services:
  proxy:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - app_network

  backend_server:
    image: ubuntu:latest
    volumes:
      - ./backend/:/backend/
    entrypoint: ["./backend/backend_script.sh"]
    networks:
      - app_network

networks:
  app_network:
    driver: bridge